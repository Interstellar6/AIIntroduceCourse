# 人工智能导论 · 如何用 AI（三节课材料）

给「人工智能导论」第 1 到第 3 节课准备的完整材料。

**一个网页文档站，就是全部内容。** 打开 `index.html` 即可——双击、离线可用、不需要服务器。
第 1 节课要讲的概念，和第 2、3 节课要动手装的步骤，都在里面。

---

## 文档站结构（19 页）

左边目录按「先原理、后动手」分成两组，顺序就是建议的讲解顺序。

### 第 1 部分 · 原理（第 1 节课讲的部分）

| 页面 | 内容 |
|---|---|
| `index.html` | 手册首页 · 两部分路线图 · 三节课安排 · 准备清单 |
| `c-vibe.html` | **开始部分的第二页**：vibe coding 的来历（Karpathy，2025-02）· 为什么它不够用 · vibe engineering → agentic engineering · 从纯 vibe 到工程化的光谱 · 这份手册要教你的三个转变 |
| `c-agent.html` | 模型是什么（三个「没有」）· Agent 是什么（循环/工具/日志/闸门）· 对比表 · 你该用哪个 |
| `c-prompt.html` | **怎么跟 Agent 说话**：它不知道的三件事 · 目标/边界/验收三要素 · 第一句话骨架 · 好说法与差说法对照 · **反馈质量四级图** · 何时该给步骤 · 和聊天框的五个不同 · 迭代四步（含「卡住三次就改需求」）· 五句可套用的话 · 说「为什么」比说「做什么」更有用 |
| `c-session.html` | 轮次与步骤 · 上下文窗口 · 压缩 · 四个会话操作 · 计划模式与目标模式 · 子代理 |
| `c-paradigms.html` | **三种经典范式**：ReAct（边想边做）· Plan-and-Solve（先谋后动）· Reflection（做完再批判）· 三者对比 · 它们对应产品里的哪些功能 |
| `c-loop.html` | **Agent 循环五步图** · 两个关键点 · Loop Engineering 的四个真问题 · 为什么「真实结果」最重要 |
| `c-harness.html` | Harness 是什么 · 八项组成 · **四层关系图** · Agent 模式（标准/极简/PTC/创造）· 一切皆插件 |
| `c-skills.html` | **怎么用 Skill**：按需加载机制 · 目录结构与 SKILL.md 例子 · 两种触发方式 · 三个存放位置 · 四个进阶用法（动态注入/传参/子代理/预授权）· 学生可写的三个 skill · 三个误区 |
| `c-tools.html` | 五个名字一张表 · harness 与配件的区分 · 手感差异 · 怎么选 |

### 第 2 部分 · 动手（第 2、3 节课做的部分）

| 页面 | 内容 |
|---|---|
| `api.html` | 注册 / 实名 / 充值 / 创建 Key / 第一次 curl 调用 / 价格与峰谷 / Key 安全 |
| `node.html` | Node.js 安装（mac / win）· 版本要求 · 国内镜像 · 权限坑 |
| `dsh.html` | DeepSeek Harness 安装启动 · 配模型 · 选工作区 · 第一个任务 · 极简模式对比 |
| `claude-code.html` | 界面选择（终端 / 编辑器扩展 / 桌面 App 能不能用）+ 安装（mac / win）+ 接 DeepSeek 的 9 个变量 + 写进 settings.json + **VS Code 扩展配置** |
| `codex.html` | Codex 安装（mac / win）+ 官方一键脚本 + **图形界面版（共用同一份配置）** + 沙箱与审批 + 手工配置 |
| `cc-switch.html` | 配置文件是怎么回事 · cc-switch 安装（mac / win）· 使用流程 · 它改了哪些文件 |
| `relay.html` | 想用别的模型：两家中转站推荐（**只推荐，不含使用教程**）+ 风险 |
| `tasks.html` | 三个练习任务 · 收尾总结 |
| `help.html` | 排错对照表 · 术语表 · 命令速查 · 课后资料 |

### 文档站的功能

- **右上角一键切换 macOS/Linux ↔ Windows**，全站命令跟着变。链接后面加 `?os=win` 可直接指定，方便发给用 Windows 的同学
- 代码块一键复制；准备清单和任务要求**可勾选，进度存在本机浏览器**里
- **十二张手绘 SVG 插图**：vibe coding 光谱图、反馈质量四级图、skill 按需加载图、Agent 循环五步图、模型 vs Agent 对比、上下文压缩前后对比、三要素与一次调用、协议三种信封、人机分工循环、三种范式对比、Claude Code 一份配置两个界面、Codex 一份配置三种界面。全部是内联矢量图，跟着明暗模式自动变色，放大不糊、可离线
- 明暗模式 · 手机浏览 · 打印 / 导出 PDF
- 加页面只要在 `assets/docs.js` 顶部的 `NAV` 数组里加一行

### 插图列表

| 图 | 位置 | 说明 |
|---|---|---|
| Agent 循环五步 | `c-loop.html` | 五步 + 回环箭头 + 「不再需要工具」出口 |
| 模型 vs Agent | `c-agent.html` | 左右对比：只有文字进出 vs 一双手加一个循环 |
| 上下文压缩前后 | `c-session.html` | 五个彩色横条 → 摘要 + 腾出来的空间 |
| 三要素 → 一次调用 | `api.html` | base_url / key / model id 三合一，发给服务器再返回 |
| 三种协议信封 | `api.html` | Claude Code / Codex / 其他工具 → 同一个模型 |
| 人机分工循环 | `tasks.html` | 蓝色是你的活、灰色是它的活，四段循环 |
| 三种范式对比 | `c-paradigms.html` | 三列并排：ReAct 绕圈、Plan-and-Solve 走直线、Reflection 原地打磨 |
| vibe coding 光谱 | `c-vibe.html` | 纯 vibe coding → AI 辅助 → agentic engineering，底下一条轴标出代价 |
| skill 按需加载 | `c-skills.html` | 左边常驻上下文（只有名字+描述）⇄ 右边磁盘上的 SKILL.md 正文 |
| 反馈质量四级 | `c-prompt.html` | 从「还是不行」到「完整报错+期望+实际」，右侧标出它能否据此行动 |
| 一份配置两个界面 | `claude-code.html` | settings.json → 终端版 / VS Code 扩展；桌面 App 用断线加 ✕ 标出不通 |
| 一份配置三种界面 | `codex.html` | ~/.codex/config.toml → CLI / 编辑器扩展 / ChatGPT 桌面端 |

改图只需要编辑对应 HTML 里的 `<figure class="fig"><svg>…</svg></figure>`；样式类在 `assets/docs.css` 的「插图」一节。

---

## 幻灯片（已归档）

`slides/` 里是更早做的一版课堂幻灯片（39 + 23 + 18 + 4 页），内容已经全部并入文档站。
留着是为了万一想投影讲——每页带讲稿备注，按 `S` 呼出，`←→` 翻页，`P` 导出 PDF。

**不需要的话直接删掉 `slides/` 整个目录即可**，文档站不依赖它。

---

## 目录结构

```
.
├── index.html … help.html        文档站（19 页）
├── assets/docs.css, docs.js      文档站样式与脚本
├── slides/                       早期幻灯片（归档，可删）
└── 参考资料/                      事实核查原始材料（约 170KB）
    ├── DeepSeek_API_事实清单_2026-09-18.md
    ├── CLI工具调研-事实清单.md
    └── API中转站-事实清单.md
```

---

## 请务必先看这一条

**所有事实的核查日期是 2026-09-18。** 这个领域半年前的材料就可能失效，
网上大量教程还停留在更早的状态。讲之前建议核对这几项：

| 易过时项 | 2026-09-18 的状态 |
|---|---|
| DeepSeek 模型名 | `deepseek-chat` / `deepseek-reasoner` 已于 **2026-07-24 停用**，现在是 `deepseek-flash` 和 `deepseek-v4-pro` |
| DeepSeek base_url | 官方文档一律写 `https://api.deepseek.com`，**不带 `/v1`** |
| Codex 协议 | **只支持 Responses**，旧的 `wire_api = "chat"` 已被删除，写上会直接启动失败 |
| DeepSeek 价格 | 会变；高峰是周一至周五 9:00–12:00、14:00–18:00，价格翻倍 |
| 各工具安装命令 | 官方安装脚本地址、cc-switch 版本号更新很快，以官方文档为准 |

三份 `参考资料/` 里的清单带**逐条来源 URL** 和 **「未核实」标注**，改动内容前先对照它们。

---

## 关于图形界面版（2026-09-18 核实）

两个工具都有图形界面，但能不能接 DeepSeek 差别很大，课件里已按下面的事实写好：

| 工具 | 形态 | 能接 DeepSeek 吗 |
|---|---|---|
| Claude Code | 终端版（CLI） | 能 |
| Claude Code | VS Code / Cursor 扩展 | 能，但**必须先在设置里勾上 `claudeCode.disableLoginPrompt`**，否则会一直要求登录 Anthropic 账号 |
| Claude Code | JetBrains 插件 | 官方有插件，但本文没有实测接第三方模型的步骤，课件里标了「没验证」 |
| Claude Code | 桌面 App | **不能**。官方文档只提供 Anthropic 账号登录这条路，且需要付费订阅 |
| Codex | 命令行 / VS Code 扩展 / ChatGPT 桌面端 | 都能，**三种形态共用 `~/.codex` 一份配置**，跑一次配置脚本就全覆盖 |

关键差异：**Codex 的图形界面是「免费附赠」的，Claude Code 的扩展需要多关一个开关，桌面 App 则完全走不通。**
上课时可以按这个顺序讲：先装 Codex 的 VS Code 扩展（最省事），再考虑 Claude Code。

---

## 两个实测发现（上课前值得知道）

1. **`kedaya.ai` 的网页对来自中国大陆的访问返回 403**，页面明确提示该地区不开放；
   但它的 **API 域名是可访问的**。也就是说：大陆学生可能**注册不了**（需要海外网络环境），
   拿到 Key 之后却能正常调用。上课前最好自己先试一次，或者提前跟学生说明。
2. **`muzeai.top` 的网页和 API 都正常可访问**。实测它同时支持 `/v1/chat/completions`、
   `/v1/messages`（Anthropic 格式）和 `/v1/responses`（Codex 用的格式）三个端点——也就是说
   Codex 和 Claude Code 都能直连它。这一点没有写进 `relay.html`，因为按你的要求
   只做推荐、不教用法；需要时可以口头补充。

---

## 一句话提醒

`relay.html` 里那两个链接带的是推广（affiliate）参数。考虑在上课时口头说明一句——
学生知道老师从中获得推荐收益，和不知道，感受是不一样的。
